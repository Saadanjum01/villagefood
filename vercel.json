{
  "version": 2,
  "installCommand": "yarn install --frozen-lockfile && yarn --cwd frontend install --frozen-lockfile",
  "buildCommand": "yarn --cwd frontend build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
